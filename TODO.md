- [x] Measure full-replay sequence lengths with `scripts/estimate_context_window.py`. The estimator counts live entity instances and cumulative upgrades at every timestep for both perspectives, includes model delimiter and end tokens, and writes the aggregate and per-replay results to `scripts/output/context_window_estimate.json`.
- [x] Diagnose overfitV2 late-run memory behavior. Two runs stopped deterministically at epoch 27, step 889 on the project's reserved-memory guard, not a raw CUDA OOM: live allocation was only about 0.24 GiB after the step while cached reservation reached about 7.59 GiB as the epoch's shuffled dynamic batch shapes accumulated reusable segments. The launcher allocator override reproduced the same failure and was removed. The guard now trims reclaimable cache at the ceiling and aborts only if the post-trim reservation remains unsafe; step telemetry continues to expose allocation, reservation, inactive splits, device-wide use, and the device-minus-reserved gap.
- [x] Prevent metrics viewers and transient sync locks from terminating training. The resumed overfitV2 run passed the former CUDA failure and completed epoch 27, then hit `PermissionError: [Errno 13]` while appending `epoch_metrics.csv`. Epoch/interval writes now retry and switch to a full-history timestamped continuation on a persistent lock; subsequent writes and resumes retain that continuation path.
- [x] Ensure that the pre-training and fine-tuning pipelines both tokenize every replay from exactly two perspectives. Both pipelines now require and record `p1,p2`, create one window stream with each player as self, and split by replay before perspective expansion so both views remain in the same train/dev/test partition.
- [x] Add opt-in raw canvas text exports to `src/thesis_ml/viz/diagnostics.py`. Passing `--txt` writes each final predicted canvas and ground-truth canvas to its own position-preserving `.txt` file; omitting it writes no text files.
- [x] Add opt-in final-canvas top-10 logit diagnostics. Passing `--json` performs a final forward pass conditioned on the completed canvas and writes `canvas_logits.json` with each sequence index, predicted token, ground-truth token, and top-10 token IDs/names, raw logits, and softmax confidence values; omitting it adds no forward pass and writes no JSON.
- [ ] Look into how there may be problems with some vocab tokens appearing significantly more frequently than others. Look into writing a script to count up how many tokens appear throughout the corpus. Look into if language pre-training does anything about this. Like more common tokens like the, and, it, ing, appearing more frequently being problematic? Can dataset size overcome these super common tokens causing model collapse of prior distribution being over-run by these common tokens? With my small dataset size, do I need to look into doing anything about my super common tokens potentially over-running my prior distribution and causing the collapse I saw.
